apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      containers:
        - name: backend
          image: tathagata16/restaurant-backend:latest

          ports:
            - containerPort: 4000

          envFrom:
            - secretRef:
                name: backend-secret